@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <img src="~/images/logo.jpg" alt="Logo" class="img-fluid mb-4">
            <h1 class="display-4">Welcome to Our Retail Store</h1>
            <p class="lead">Discover our latest products and deals.</p>
            <p>Explore our collection of high-quality items for every need.</p>
            <a href="@Url.Action("Index", "Admin")" class="btn btn-primary btn-lg">Admin</a>
            <a href="@Url.Action("Index", "User")" class="btn btn-primary btn-lg">User</a>
        </div>
    </div>
</div>
